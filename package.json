{
  "name": "overseer-broadcast",
  "version": "0.0.0",
  "description": "View a network of local raspberry pi cameras.",
  "main": "app.js",
  "scripts": {
    "broadcast": "export NODE_ENV=production && forever start -c /bin/bash broadcaster-src/record.sh",
    "deploy-broadcast": "forever stopall && git pull && npm run broadcast",
    "start": "forever start overseer-src/server.js",
    "stop": "forever stop overseer-src/server.js",
    "dev": "nodemon overseer-src/server.js",
    "deploy": "npm stop && git pull && npm start"
  },
  "author": "sheodox",
  "dependencies": {
    "express": "^4.17.1",
    "pug": "^2.0.4",
    "request": "^2.88.0"
  }
}
